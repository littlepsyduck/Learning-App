<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context=".UserSettingsActivity">

    <RelativeLayout
        android:id="@+id/title_bar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:padding="16dp">

        <ImageView
            android:id="@+id/ivBack"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:src="@drawable/ic_back_arrow" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="Account"
            android:textColor="@color/black"
            android:textSize="18sp"
            android:textStyle="bold" />
    </RelativeLayout>
    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/title_bar"
        android:background="@color/duo_border_grey" />

    <LinearLayout
        android:id="@+id/bottom_buttons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical"
        android:padding="16dp">

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnSave"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/button_background_green"
            android:text="SAVE"
            android:textColor="@android:color/white"
            android:textStyle="bold" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnLogout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:background="@drawable/button_background_white_outlined"
            android:text="LOGOUT"
            android:textColor="@color/duo_green"
            android:textStyle="bold" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnDelete"
            style="@style/Widget.AppCompat.Button.Borderless"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="16dp"
            android:text="DELETE MY ACCOUNT"
            android:textColor="@color/reg_error_red"
            android:textStyle="bold" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/divider"
        android:layout_above="@id/bottom_buttons"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">
            <TextView
                style="@style/RegistrationLabel"
                android:text="Profile picture" />

            <de.hdodenhof.circleimageview.CircleImageView
                android:id="@+id/ivAvatarPreview"
                android:layout_width="96dp"
                android:layout_height="96dp"
                android:layout_gravity="center_horizontal"
                android:layout_marginBottom="16dp"
                android:visibility="gone"
                android:src="#A020F0" />

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/btnChooseFile"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:text="CHOOSE FILE"
                android:background="@drawable/button_background_white_outlined"
                android:textColor="@color/duo_green" />

            <TextView
                android:id="@+id/tvFileSelected"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="8dp"
                android:text="no file selected"
                android:textColor="@color/duo_grey" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="4dp"
                android:text="maximum image size is 1 MB"
                android:textColor="@color/duo_grey"
                android:textSize="12sp"/>

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginTop="16dp"
                android:layout_marginBottom="16dp"
                android:background="@color/duo_border_grey" />
            <TextView
                style="@style/RegistrationLabel"
                android:text="Họ và tên" />
            <EditText
                android:id="@+id/etFullName"
                style="@style/RegistrationEditText"
                android:hint="Họ và tên" />

            <TextView
                style="@style/RegistrationLabel"
                android:layout_marginTop="16dp"
                android:text="Username" />
            <EditText
                android:id="@+id/etUsername"
                style="@style/RegistrationEditText"
                android:hint="Username" />

            <TextView
                style="@style/RegistrationLabel"
                android:layout_marginTop="16dp"
                android:text="Đổi mật khẩu" />
            <EditText
                android:id="@+id/etCurrentPassword"
                style="@style/RegistrationEditText"
                android:hint="Mật khẩu hiện tại (bắt buộc nếu đổi)"
                android:inputType="textPassword" />
            <EditText
                android:id="@+id/etNewPassword"
                style="@style/RegistrationEditText"
                android:layout_marginTop="8dp"
                android:hint="Mật khẩu mới (để trống nếu không đổi)"
                android:inputType="textPassword" />

            <TextView
                style="@style/RegistrationLabel"
                android:layout_marginTop="16dp"
                android:text="Tuổi" />
            <EditText
                android:id="@+id/etAge"
                style="@style/RegistrationEditText"
                android:hint="Tuổi"
                android:inputType="number" />

            <TextView
                style="@style/RegistrationLabel"
                android:layout_marginTop="16dp"
                android:text="Email" />
            <EditText
                android:id="@+id/etEmail"
                style="@style/RegistrationEditText"
                android:hint="Email"
                android:inputType="textEmailAddress" />

        </LinearLayout>
    </ScrollView>
</RelativeLayout>