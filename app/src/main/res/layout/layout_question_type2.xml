<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:padding="16dp">

    <!-- Ảnh nhân vật ở trên, lệch trái -->
    <ImageView
        android:id="@+id/ivCharacter"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="16dp"
        android:src="@android:drawable/ic_menu_gallery"
        android:scaleType="fitCenter"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Answer Section - ở dưới ảnh -->
    <LinearLayout
        android:id="@+id/answerSection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/ivCharacter"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Label Answer với icon -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="8dp">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@android:drawable/star_big_on"
                android:tint="#9C27B0" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Answer"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="#9C27B0"
                android:layout_marginStart="4dp" />
        </LinearLayout>

        <!-- Khu vực sắp xếp câu (các dòng với đường thẳng) -->
        <LinearLayout
            android:id="@+id/answerContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="#FFFFFF">

            <!-- Dòng đầu tiên (nơi hiển thị câu đã sắp xếp) -->
            <LinearLayout
                android:id="@+id/answerLine1"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:minHeight="50dp"
                android:padding="8dp"
                android:background="#FFFFFF">

                <TextView
                    android:id="@+id/tvAnswerPlaceholder"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text=""
                    android:textSize="16sp"
                    android:textColor="#999999"
                    android:gravity="center"
                    android:visibility="gone" />
            </LinearLayout>
            
            <!-- Đường thẳng dưới dòng 1 (sẽ được thêm động) -->
            <View
                android:id="@+id/line1"
                android:layout_width="match_parent"
                android:layout_height="2dp"
                android:background="#000000"
                android:visibility="gone" />

            <!-- Dòng thứ 2 (sẽ được thêm động nếu cần) -->
            <LinearLayout
                android:id="@+id/answerLine2"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:minHeight="50dp"
                android:padding="8dp"
                android:background="#FFFFFF"
                android:visibility="gone" />
            
            <!-- Đường thẳng dưới dòng 2 (sẽ được thêm động) -->
            <View
                android:id="@+id/line2"
                android:layout_width="match_parent"
                android:layout_height="2dp"
                android:background="#000000"
                android:visibility="gone" />

            <!-- Dòng thứ 3 (sẽ được thêm động nếu cần) -->
            <LinearLayout
                android:id="@+id/answerLine3"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:minHeight="50dp"
                android:padding="8dp"
                android:background="#FFFFFF"
                android:visibility="gone" />
            
            <!-- Đường thẳng dưới dòng 3 (sẽ được thêm động) -->
            <View
                android:id="@+id/line3"
                android:layout_width="match_parent"
                android:layout_height="2dp"
                android:background="#000000"
                android:visibility="gone" />
        </LinearLayout>
    </LinearLayout>

    <!-- Word Selection Section -->
    <LinearLayout
        android:id="@+id/wordSelectionSection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/answerSection"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- Label Word Selection với icon -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="8dp">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@android:drawable/star_big_on"
                android:tint="#9C27B0" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Word Selection"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="#9C27B0"
                android:layout_marginStart="4dp" />
        </LinearLayout>

        <!-- RecyclerView cho word bank -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvWordBank"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp" />
    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>

